/**
 * Power calculation using Java, comparing with python
 */

public class Power {

  /**
   * main entry for power calculation
   */
  public static void main(String[] args) {
    long ans = (long) Math.pow(2, 31);
    System.out.println(ans);

    /**
     * 1. Java requires declaring data type
     * 2. Java requires declaring a class before coding
     * 3. Java requires ';' for statement termination and '{}' for code block
     */
  }
}